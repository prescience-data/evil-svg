/**
 * This is where you write your client side code you want to execute.
 *
 * Keep the code as simple as possible to avoid payload bloat and possible
 *  breakages with the obfuscator.
 *
 * If you encounter errors, disable the obfuscation build step.
 *
 * @returns {void}
 */
( () => {

  // Evil code to inject in SVG...
  alert(`Evil SVG!`)
} )()
